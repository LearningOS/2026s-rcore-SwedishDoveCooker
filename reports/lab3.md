## 编程作业

### 关于之前的 syscall

把之前的代码稍作修改即可, 之前的那个写函数改为了复用读函数的实现, 更加简洁。

---

### 进程创建

1. 复用 new 创建一个全新的进程, 之前的地址空间都可以不用管, 而且因为不是同一个函数所以也不需要手动改子进程的寄存器。
2. 建立父子联系并且加入 `task manager` 中。

---

### stride 调度算法

将 `TaskManager` 改为 vec 实现, 每次要 fetch 时排序, `stride` 和 `prior` 字段加在 `TaskControlBlockInner` 中, 每次回到 `init` 时增加 `stride`。

## 问答作业

1. 不是, 250 + 10 已经溢出了。

2. 假如当前 STRIDE_MAX – STRIDE_MIN > BigStride / 2, 因为一次最多加 BigStride / 2, 那 STRIDE_MAX 和 STRIDE_MIN 对应的两个进程上一次执行一定是 STRIDE_MAX 对应的进程执行, 那上次执行的时候 STRIDE_MAX – STRIDE_MIN 仍然是大于 0 的数, 应该是 STRIDE_MIN 执行, 矛盾。

3.

```rust
fn partial_cmp(&self, other: &Self) -> Option<Ordering> {
        // ...
        let diff = self.0.wrapping_sub(other.0);
        if diff < HALF_U64 {
            Some(Ordering::Greater)
        } else {
            Some(Ordering::Less)
        }
    }
```

# **荣誉准则**

1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 **以下各位** 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：

   > 无

2. 此外，我也参考了 **以下资料** ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：

   > 无

3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。 我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。

4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。 我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。 我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。 我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。
